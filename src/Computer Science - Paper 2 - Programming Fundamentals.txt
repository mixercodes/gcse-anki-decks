#separator:tab
#html:true
#notetype column:1
#deck column:4
Basic	[Q3 — 3 marks] Write an SQL query to return all fields for students who take Art from a table called Results.	<pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">SELECT *<br>FROM Results<br>WHERE Subject = "Art"</pre>3 mark points: <code>SELECT *</code> (or listing all fields); <code>FROM Results</code>; <code>WHERE Subject = "Art"</code>	Computer Science::Paper 2::Programming Fundamentals
Cloze	SQL to return all fields for Art students: <code>{{c1::SELECT *}} {{c2::FROM Results}} {{c3::WHERE Subject = "Art"}}</code>. The * means {{c4::all fields/columns}}. The table name is {{c5::Results}}.		Computer Science::Paper 2::Programming Fundamentals
Basic	[Q7b — 2 marks] Identify two variables used in the circle area program.	The two variables are:<br>1. <b>radius</b><br>2. <b>area</b>	Computer Science::Paper 2::Programming Fundamentals
Cloze	In the circle area program, the two variables are {{c1::radius}} and {{c2::area}}. A variable is a named storage location whose value {{c3::can change}} during execution.		Computer Science::Paper 2::Programming Fundamentals
Basic	[Q7c(i) — 1 mark] Identify one item from the circle area program that could be written as a constant.	Acceptable answers: <b>3.142</b> (pi), <b>2</b>, <b>1</b>, or <b>30</b>.<br>These are literal values that do not change while the program runs, so they can be defined as named constants.	Computer Science::Paper 2::Programming Fundamentals
Basic	[Q7c(ii) — 1 mark] Why is it better to store a value like 3.142 as a constant rather than a literal number in the code?	Acceptable answers:<br>• The number <b>does not need to be changed while the program is running</b><br>• The number can be <b>updated once</b> and it updates throughout the entire program (no need to find and replace every occurrence)	Computer Science::Paper 2::Programming Fundamentals
Cloze	Storing 3.142 as a constant is better because: it does not need to {{c1::be changed while the program is running}}, and if it does change it can be {{c2::updated once}} and the change applies {{c3::throughout the entire program}}.		Computer Science::Paper 2::Programming Fundamentals
Basic	[Q7d — 3 marks] Which of the three programming constructs are used in the circle area program: sequence, selection, iteration?	<b>Sequence:</b> HAS been used (instructions run in order)<br><b>Selection:</b> HAS been used (an IF statement checks whether input is valid)<br><b>Iteration:</b> HAS NOT been used (there are no loops in the program)	Computer Science::Paper 2::Programming Fundamentals
Cloze	In the circle area program: Sequence — {{c1::HAS}} been used. Selection — {{c2::HAS}} been used. Iteration — {{c3::HAS NOT}} been used.		Computer Science::Paper 2::Programming Fundamentals
Basic	[Q8a — 2 marks] What data type would you use to store the number of minutes a game was played, and why?	<b>Integer</b> — because the level of accuracy needed does not require decimals; rounding to the nearest minute is sufficient / the number of seconds is not important.<br><b>OR Real/Float</b> — because the number of seconds may be important / allows fractions of a minute to be stored.<br>(Either is acceptable if the reason matches the data type.)	Computer Science::Paper 2::Programming Fundamentals
Cloze	Data type for minutes played: {{c1::Integer}} is suitable because {{c2::level of accuracy not needed / round to nearest minute}}. Alternatively {{c3::Real/Float}} is suitable if {{c4::seconds may be important / fractions of a minute need to be stored}}.		Computer Science::Paper 2::Programming Fundamentals
Cloze	[Sample Q8c — 1 mark] The 2D array <code>minsPlayed</code> stores minutes played. Dan (column 3) on Wednesday (row 2) is accessed as <code>print(minsPlayed[3,2])</code>. Write code to output Stuart's (column {{c1::0}}) minutes on Friday (row {{c2::4}}):<br><code>print(minsPlayed[{{c3::0}}, {{c4::4}}])</code>		Computer Science::Paper 2::Programming Fundamentals
Basic	[Sample Q8g(ii) — 4 marks] Complete the program to convert hours and minutes to total minutes using a function:<br><pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">hours = input("Please enter number of hours played")<br>minutes = input("Please enter number of minutes played")<br>finalTotal = ___________________________________<br>print(finalTotal)<br><br>function ___________________________________<br>    ___________________________________<br>    ___________________________________<br>    ___________________________________<br>    ___________________________________<br>endfunction</pre>	Missing parts:<br>1. <code>totalMins(hours, minutes)</code> — the function call<br>2. <code>totalMins(hours, minutes)</code> — function header (name + params)<br>3. <code>total = (hours * 60) + minutes</code> — calculation<br>4. <code>return total</code><br><br><b>Python equivalent:</b><br><pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">def totalMins(hours, minutes):<br>    total = (hours * 60) + minutes<br>    return total<br><br>hours = int(input("Please enter number of hours played"))<br>minutes = int(input("Please enter number of minutes played"))<br>finalTotal = totalMins(hours, minutes)<br>print(finalTotal)</pre>	Computer Science::Paper 2::Programming Fundamentals
Cloze	The function totalMins converts hours and minutes to total minutes. The formula inside is: <code>total = ({{c1::hours * 60}}) + {{c2::minutes}}</code>. The function ends with <code>{{c3::return total}}</code>. The result is stored by calling <code>finalTotal = {{c4::totalMins(hours, minutes)}}</code>.		Computer Science::Paper 2::Programming Fundamentals
Basic	What is the difference between a variable and a constant? Give an example from the circle area program.	<b>Variable:</b> a named storage location whose value CAN change during program execution (e.g. radius, area).<br><b>Constant:</b> a named storage location whose value is FIXED and does not change (e.g. pi = 3.142).<br>Using constants improves maintainability — if the value needs updating, change it in one place only.	Computer Science::Paper 2::Programming Fundamentals
Basic	What are the three programming constructs? Define each with an example.	<b>Sequence:</b> instructions execute one after another in order — e.g. radius = input(...) then area = 3.142 * radius^2.<br><b>Selection:</b> a decision is made using IF/ELSE — e.g. if radius &lt; 1 then print("invalid").<br><b>Iteration:</b> a block of code repeats using a loop — e.g. while, for. Not used in the circle program.	Computer Science::Paper 2::Programming Fundamentals
Basic	[2024 Q1 — 3 marks] Identify the construct used by each: if/then/endif; for/next; while/endwhile.	<code>if/then/endif</code> → <b>Selection</b><br><code>for/next</code> → <b>Iteration</b><br><code>while/endwhile</code> → <b>Iteration</b>	Computer Science::Paper 2::Programming Fundamentals
Cloze	<code>if/then/endif</code> is an example of {{c1::Selection}}. <code>for/next</code> is an example of {{c2::Iteration}}. <code>while/endwhile</code> is an example of {{c3::Iteration}}.		Computer Science::Paper 2::Programming Fundamentals
Basic	[2024 Q6 — 3 marks] Given message = "abcd1234": what does message.upper return? What does message.left(4) return? What does int(message.right(4)) * 2 return?	<code>message.upper</code> → <b>"ABCD1234"</b><br><code>message.left(4)</code> → <b>"abcd"</b><br><code>int(message.right(4)) * 2</code> → <code>int("1234") * 2</code> → <b>2468</b>	Computer Science::Paper 2::Programming Fundamentals
Cloze	For message = "abcd1234": <code>message.upper</code> → {{c1::"ABCD1234"}}. <code>message.left(4)</code> → {{c2::"abcd"}}. <code>int(message.right(4)) * 2</code> → {{c3::2468}}.		Computer Science::Paper 2::Programming Fundamentals
Basic	[2024 Q9a(i) — 3 marks] State the most appropriate data type for: teamName, studentYearGroup, javelinThrow.	<b>teamName</b> → String (stores text)<br><b>studentYearGroup</b> → Integer (whole number, e.g. 10 or 11)<br><b>javelinThrow</b> → Real/Float (distance in metres, may have decimal places)	Computer Science::Paper 2::Programming Fundamentals
Cloze	Data types: teamName → {{c1::String}}; studentYearGroup → {{c2::Integer}}; javelinThrow → {{c3::Real/Float}}.		Computer Science::Paper 2::Programming Fundamentals
Basic	[2024 Q9a(ii) — 4 marks] The array <code>theTeam</code> stores 6 student names (index 0–5): Ali, Eve, Ling, Nina, Sarah, Tom. Complete the linear search function:<br><pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">function linearSearch(studentName)<br>    for count = 0 to ____<br>        if theTeam[____] == ____ then<br>            return ____<br>        endif<br>    next count<br>    return False<br>endfunction</pre>	Missing parts:<br>1. <code>5</code> (end of loop — array has indices 0–5)<br>2. <code>count</code> (array index)<br>3. <code>studentName</code> (comparison value)<br>4. <code>True</code> (found)<br><br><b>Python equivalent:</b><br><pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">def linearSearch(studentName):<br>    for count in range(6):<br>        if theTeam[count] == studentName:<br>            return True<br>    return False</pre>	Computer Science::Paper 2::Programming Fundamentals
Cloze	Linear search function: loop <code>for count = {{c1::0}} to {{c2::5}}</code>. Inside: <code>if theTeam[count] {{c3::== studentName}} then return {{c4::True}}</code>. After loop: <code>return {{c5::False}}</code>.		Computer Science::Paper 2::Programming Fundamentals
Cloze	[2024 Q9b — 4 marks] Trace this algorithm when yearGroup=10 and javelinThrow=14.3:<br><pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">01 javelinThrow = input("Enter distance")<br>02 yearGroup = input("Enter year group")<br>03 if javelinThrow &gt;= 20.0 then<br>04     score = 3<br>05 elseif javelinThrow &gt;= 10.0 then<br>06     score = 2<br>07 else<br>08     score = 1<br>09 endif<br>10 if yearGroup != 11 then<br>11     score = score * 2<br>12 endif<br>13 print("The score is", score)</pre><table border="1" cellpadding="6" style="border-collapse:collapse;font-size:13px;"><tr><th>Line</th><th>javelinThrow</th><th>yearGroup</th><th>score</th><th>Output</th></tr><tr><td>01</td><td>{{c1::14.3}}</td><td></td><td></td><td></td></tr><tr><td>02</td><td></td><td>{{c2::10}}</td><td></td><td></td></tr><tr><td>06</td><td></td><td></td><td>{{c3::2}}</td><td></td></tr><tr><td>11</td><td></td><td></td><td>{{c4::4}}</td><td></td></tr><tr><td>13</td><td></td><td></td><td></td><td>{{c5::"The score is 4"}}</td></tr></table>Line 03: 14.3 &gt;= 20? {{c6::No}}. Line 05: 14.3 &gt;= 10? {{c7::Yes}} → score=2. Line 10: yearGroup != 11? {{c8::Yes}} → score×2=4.		Computer Science::Paper 2::Programming Fundamentals
Basic	[2024 Q9c(i) — 3 marks] Write a validation algorithm that inputs a height and prints "VALID" if 40.0 ≤ height ≤ 180.0, otherwise "NOT VALID".	<b>OCR pseudocode:</b><br><pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">height = input("Enter height")<br>if height &gt;= 40.0 AND height &lt;= 180.0 then<br>    print("VALID")<br>else<br>    print("NOT VALID")<br>endif</pre>Key mark points: input; if with AND condition; correct boundaries (40.0 and 180.0); both print statements; endif.<br><b>Python equivalent:</b><br><pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">height = float(input("Enter height"))<br>if height &gt;= 40.0 and height &lt;= 180.0:<br>    print("VALID")<br>else:<br>    print("NOT VALID")</pre>	Computer Science::Paper 2::Programming Fundamentals
Cloze	Height validation: <code>height = {{c1::input("Enter height")}}</code>. Condition: <code>if height {{c2::>= 40.0 AND height <= 180.0}} then</code>. Print {{c3::"VALID"}} if true; print {{c4::"NOT VALID"}} in else branch.		Computer Science::Paper 2::Programming Fundamentals
Basic	[2024 Q9d — 3 marks] Write an SQL query to return StudentID and TeamName for all year 11 students from the table TblResult.	<pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">SELECT StudentID, TeamName<br>FROM TblResult<br>WHERE YearGroup = 11</pre>3 mark points: <code>SELECT StudentID, TeamName</code>; <code>FROM TblResult</code>; <code>WHERE YearGroup = 11</code>	Computer Science::Paper 2::Programming Fundamentals
Cloze	SQL for year 11 students: <code>{{c1::SELECT StudentID, TeamName}}</code> / <code>{{c2::FROM TblResult}}</code> / <code>{{c3::WHERE YearGroup = 11}}</code>	Computer Science::Paper 2::Programming Fundamentals
Basic	[2024 Q9e(i) — 1 mark] The program only stores relevant student data (name, year group, throw distance). What computational thinking technique is this?	<b>Abstraction</b> — removing or hiding irrelevant details (e.g. student address, photo, other subjects) to focus only on the data needed to solve the problem.	Computer Science::Paper 2::Programming Fundamentals
Basic	[2024 Q9e(ii) — 1 mark] The problem is broken into sub-problems: one function calculates the score, another performs the search. What computational thinking technique is this?	<b>Decomposition</b> — breaking a complex problem into smaller, more manageable sub-problems (e.g. a separate function for score calculation, a separate function for searching the array).	Computer Science::Paper 2::Programming Fundamentals
Cloze	Hiding irrelevant details to reduce complexity is called {{c1::Abstraction}}. Breaking a complex problem into smaller sub-problems is called {{c2::Decomposition}}.		Computer Science::Paper 2::Programming Fundamentals
Basic	[2023 Q1b — 1 mark] Write pseudocode to add num1 and num2 and store the result in total.	<code>total = num1 + num2</code>	Computer Science::Paper 2::Programming Fundamentals
Basic	[2023 Q1c(i) — 1 mark] What operator raises 12 to the power of 2 in pseudocode: print(12 __ 2)?	The operator is <b>^</b> (caret = exponentiation/power).<br>Full code: <code>print(12 ^ 2)</code> → outputs 144.	Computer Science::Paper 2::Programming Fundamentals
Basic	[2023 Q1c(ii) — 1 mark] What operator checks if a number is odd or even: if number __ 2 == 0?	The operator is <b>MOD</b> (modulus — gives the remainder after division).<br>Full code: <code>if number MOD 2 == 0 then print("Even number")</code>	Computer Science::Paper 2::Programming Fundamentals
Cloze	To check if a number is even: <code>if number {{c1::MOD}} 2 {{c2::== 0}} then</code> print "Even". MOD gives the {{c3::remainder}} after division. To raise to a power use {{c4::^}} e.g. <code>12 ^ 2</code> = 144.		Computer Science::Paper 2::Programming Fundamentals
Basic	[2023 Q6a — 4 marks] State the most appropriate data type for each variable: UserName, EmergencyPhoneNumber, DoorSensorActive, DoorActiveTime.	<b>UserName</b> → String (e.g. "Admin123" — contains letters and numbers)<br><b>EmergencyPhoneNumber</b> → String (e.g. "+449999999999" — contains + symbol, cannot be used arithmetically)<br><b>DoorSensorActive</b> → Boolean (True/False)<br><b>DoorActiveTime</b> → Integer (whole number of seconds)	Computer Science::Paper 2::Programming Fundamentals
Cloze	Data types: UserName → {{c1::String}}; EmergencyPhoneNumber → {{c2::String}} (contains + symbol); DoorSensorActive → {{c3::Boolean}}; DoorActiveTime → {{c4::Integer}}.		Computer Science::Paper 2::Programming Fundamentals
Basic	[2023 Q6c — 5 marks] Use this program to answer questions (i)–(v):<br><pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">01 sensorType = input("Enter code of the type of sensor to reset")<br>02 if(CheckSensorCode(sensorType)) then<br>03     sensorNumber = input("Please input the number of the sensor to reset")<br>04     sensorID = sensorType + sensorNumber<br>05     ResetSensor(sensorID)<br>06 endif</pre>(i) Which line has concatenation? (ii) Name a variable identifier. (iii) What data type does CheckSensorCode() return? (iv) Which line has a function call? (v) Name two programming constructs used.	(i) Line <b>04</b> — <code>sensorID = sensorType + sensorNumber</code><br>(ii) Any of: <b>sensorType</b>, sensorNumber, sensorID<br>(iii) <b>Boolean</b> — it returns True/False (used as the if condition)<br>(iv) Line <b>02</b> (CheckSensorCode) or line <b>05</b> (ResetSensor)<br>(v) <b>Sequence</b> and <b>Selection</b> (the if statement on line 02)	Computer Science::Paper 2::Programming Fundamentals
Basic	[2023 Q6d — 3 marks] Write an SQL statement to display sensor IDs of door sensors triggered for more than 20 seconds from the events table.	<pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">SELECT SensorID<br>FROM events<br>WHERE SensorType = "Door" AND Length &gt; 20</pre>3 mark points: <code>SELECT SensorID</code>; <code>FROM events</code>; <code>WHERE SensorType = "Door" AND Length &gt; 20</code>	Computer Science::Paper 2::Programming Fundamentals
Cloze	SQL for door sensors triggered &gt;20 seconds: <code>{{c1::SELECT SensorID}}</code> / <code>{{c2::FROM events}}</code> / <code>{{c3::WHERE SensorType = "Door" AND Length > 20}}</code>	Computer Science::Paper 2::Programming Fundamentals
Basic	[2023 Q6f(i) — 1 mark] What is the process called that converts integer data to string data?	<b>Casting</b> (also called type conversion or type casting).<br>Example: <code>str(100)</code> converts the integer 100 to the string "100".	Computer Science::Paper 2::Programming Fundamentals
Basic	[2022 Q1a — 4 marks] Classify each code snippet as Selection or Iteration: for/next loop; while/endwhile loop; if/endif; switch/endswitch.	<code>for i = 1 to 10 / next i</code> → <b>Iteration</b><br><code>while score != 0 / endwhile</code> → <b>Iteration</b><br><code>if playerHit() then / endif</code> → <b>Selection</b><br><code>switch bonus: / endswitch</code> → <b>Selection</b>	Computer Science::Paper 2::Programming Fundamentals
Cloze	Constructs: <code>for/next</code> → {{c1::Iteration}}; <code>while/endwhile</code> → {{c2::Iteration}}; <code>if/endif</code> → {{c3::Selection}}; <code>switch/endswitch</code> → {{c4::Selection}}.		Computer Science::Paper 2::Programming Fundamentals
Basic	[2022 Q1b — 1 mark] Write pseudocode to increment the variable score by one.	<code>score = score + 1</code>	Computer Science::Paper 2::Programming Fundamentals
Basic	[2022 Q1c — 2 marks] Name the computational thinking technique for each: (1) breaking a complex problem into smaller problems; (2) hiding irrelevant details to reduce complexity.	1. Breaking into smaller problems → <b>Decomposition</b><br>2. Hiding irrelevant details → <b>Abstraction</b>	Computer Science::Paper 2::Programming Fundamentals
Basic	[2022 Q5a(i) — 2 marks] State the most appropriate data type for: nights (number of nights stayed, 1–5); room ("basic" or "premium").	<b>nights</b> → Integer (whole number between 1 and 5)<br><b>room</b> → String (stores text value "basic" or "premium")	Computer Science::Paper 2::Programming Fundamentals
Basic	[2022 Q5a(iii) — 4 marks] The SQL below to display bookings over 1 night has three errors. Identify and correct them:<br><pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">SELECT ALL<br>FROM TblBookings<br>IF Nights &lt; 1</pre>	Corrected SQL:<br><pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">SELECT *<br>FROM TblBookings<br>WHERE Nights &gt; 1</pre>Errors fixed: <code>SELECT ALL</code> → <code>SELECT *</code>; <code>IF</code> → <code>WHERE</code>; <code>Nights &lt; 1</code> → <code>Nights &gt; 1</code>	Computer Science::Paper 2::Programming Fundamentals
Cloze	Corrected SQL for bookings over 1 night: <code>{{c1::SELECT *}}</code> / <code>{{c2::FROM TblBookings}}</code> / <code>{{c3::WHERE Nights > 1}}</code>. The original errors were: SELECT ALL → SELECT *; IF → {{c4::WHERE}}; Nights &lt; 1 → {{c5::Nights > 1}}.		Computer Science::Paper 2::Programming Fundamentals
Basic	[2022 Q2d(i) — 2 marks] What is casting? Give the line number where it is used in the staffID algorithm.	<b>Casting</b> is converting a value from one data type to another (type conversion).<br>Example: <code>str(year)</code> converts the integer year to a string so it can be concatenated with the string surname.<br>Line number: <b>03</b> (<code>staffID = surname + str(year)</code>)	Computer Science::Paper 2::Programming Fundamentals
Cloze	Casting is converting a value from one {{c1::data type}} to another. In the staffID algorithm, <code>{{c2::str(year)}}</code> on line {{c3::03}} casts the integer year to a {{c4::String}} so it can be concatenated with surname.		Computer Science::Paper 2::Programming Fundamentals
