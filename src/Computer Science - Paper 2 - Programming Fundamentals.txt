#separator:tab
#html:true
#notetype column:1
#deck column:4
Cloze	OCR data types: {{c1::Integer}} (whole numbers, e.g. 42, -7), {{c2::Real/Float}} (decimal numbers, e.g. 3.14), {{c3::Boolean}} (True or False only), {{c4::Character}} (single character in single quotes, e.g. 'A'), {{c5::String}} (sequence of characters in double quotes, e.g. "Hello"). The correct data type reduces {{c6::memory}} use and prevents errors.		Computer Science::Paper 2::Programming Fundamentals
Basic	What is the difference between a variable and a constant? Give examples.	<b>Variable:</b> a named storage location whose value <i>can change</i> during program execution.<br><code>score = 0</code> then later <code>score = score + 10</code><br><b>Constant:</b> a named storage location whose value is <i>fixed</i> — it cannot be changed once assigned. Declared using <code>const</code> in many languages.<br><code>const PI = 3.14159</code><br><b>Why use constants?</b> Improves readability (PI is clearer than 3.14159), prevents accidental changes, makes updates easier (change value in one place).	Computer Science::Paper 2::Programming Fundamentals
Basic	What is casting (type conversion)? Give three examples.	Casting converts a value from one data type to another. Essential because user input is always returned as a string.<br><b>int("42")</b> → integer 42<br><b>float("3.14")</b> → float 3.14<br><b>str(100)</b> → string "100"<br><b>bool(0)</b> → False (0 = False, any non-zero = True)<br>Without casting, attempting arithmetic on a string causes a type error.	Computer Science::Paper 2::Programming Fundamentals
Cloze	OCR pseudocode string operations: {{c1::length("hello")}} returns {{c2::5}}. {{c3::substring("hello", 1, 3)}} returns {{c4::"ell"}} (start index 1, length 3). {{c5::upper("hello")}} → "HELLO". {{c6::lower("HELLO")}} → "hello". Concatenation: "Hi" {{c7::+}} " there" → "Hi there".		Computer Science::Paper 2::Programming Fundamentals
Basic	Write pseudocode to access individual characters in a string.	<pre style="background:#f0f0f0;padding:8px;border-radius:4px;font-family:monospace;font-size:13px;">name = "Alice"<br>print(name[0])      // outputs "A"<br>print(name[4])      // outputs "e"<br>print(length(name)) // outputs 5<br>print(name[1:3])    // outputs "li" (substring)</pre>Strings are indexed from <b>0</b>. The last character is at index <b>length-1</b>.	Computer Science::Paper 2::Programming Fundamentals
Basic	What is a 1D array? Write pseudocode to declare one and access elements.	A 1D array stores multiple values of the <b>same data type</b> under one variable name, accessed by index (starting at 0).<br><pre style="background:#f0f0f0;padding:8px;border-radius:4px;font-family:monospace;font-size:13px;">array scores[5]<br>scores[0] = 85<br>scores[1] = 72<br>scores[2] = 91<br>scores[3] = 68<br>scores[4] = 79<br>print(scores[2])    // outputs 91</pre>	Computer Science::Paper 2::Programming Fundamentals
Basic	What is a 2D array? Write pseudocode to declare and access it.	A 2D array is an array of arrays — like a grid/table. Accessed using two indices: [row][column].<br><pre style="background:#f0f0f0;padding:8px;border-radius:4px;font-family:monospace;font-size:13px;">array grid[3][3]<br>grid[0][0] = 1<br>grid[0][1] = 2<br>grid[1][2] = 5<br>print(grid[1][2])   // outputs 5<br><br>// Iterate all elements:<br>for row = 0 to 2<br>    for col = 0 to 2<br>        print(grid[row][col])<br>    next col<br>next row</pre>	Computer Science::Paper 2::Programming Fundamentals
Basic	What is the difference between a function and a procedure?	<b>Procedure:</b> a named, reusable block of code that performs a task. Does <b>NOT</b> return a value. Called by name alone.<br><b>Function:</b> a named, reusable block of code that performs a task and <b>DOES return a value</b> using the <code>return</code> keyword.<br>Both can accept <b>parameters</b> (input values).<br><b>Benefits of both:</b> code reuse (write once, use many times), easier testing, shorter/cleaner code, modular design.	Computer Science::Paper 2::Programming Fundamentals
Basic	Write OCR pseudocode for a function with parameters and a return value.	<pre style="background:#f0f0f0;padding:8px;border-radius:4px;font-family:monospace;font-size:13px;">function multiply(a, b)<br>    return a * b<br>endfunction<br><br>result = multiply(4, 5)<br>print(result)    // outputs 20</pre>The function receives parameters <code>a</code> and <code>b</code>, calculates the product, and <b>returns</b> it to the calling code.	Computer Science::Paper 2::Programming Fundamentals
Basic	Write OCR pseudocode for a procedure with parameters.	<pre style="background:#f0f0f0;padding:8px;border-radius:4px;font-family:monospace;font-size:13px;">procedure greet(name)<br>    print("Hello, " + name + "!")<br>endprocedure<br><br>greet("Alice")    // outputs: Hello, Alice!<br>greet("Bob")      // outputs: Hello, Bob!</pre>Procedures perform actions but do <b>not return</b> a value.	Computer Science::Paper 2::Programming Fundamentals
Cloze	A {{c1::local}} variable is declared inside a function/procedure and can only be accessed {{c2::within}} that block — it ceases to exist when the function ends. A {{c3::global}} variable is declared outside all functions and can be accessed from {{c4::anywhere}} in the program. Local variables are preferred because they reduce {{c5::side effects}} and make debugging {{c6::easier}}.		Computer Science::Paper 2::Programming Fundamentals
Basic	What does MOD and DIV do? Give exam-style examples.	<b>MOD</b> returns the <b>remainder</b> after integer division.<br>17 MOD 5 = 2 (17 = 3×5 + <b>2</b>)<br>10 MOD 2 = 0 (even number check!)<br><b>DIV</b> returns the <b>quotient</b> (integer division, ignoring remainder).<br>17 DIV 5 = 3<br>10 DIV 3 = 3<br><b>Exam uses:</b> checking if a number is even (n MOD 2 == 0), extracting digits (n DIV 10 gives all digits except the last).	Computer Science::Paper 2::Programming Fundamentals
Basic	Write pseudocode to find the largest value in an array of 5 integers.	<pre style="background:#f0f0f0;padding:8px;border-radius:4px;font-family:monospace;font-size:13px;">array nums[5] = [3, 7, 2, 9, 4]<br>largest = nums[0]<br>for i = 1 to 4<br>    if nums[i] &gt; largest then<br>        largest = nums[i]<br>    endif<br>next i<br>print(largest)    // outputs 9</pre>	Computer Science::Paper 2::Programming Fundamentals
Basic	Write pseudocode to calculate the total and average of an array.	<pre style="background:#f0f0f0;padding:8px;border-radius:4px;font-family:monospace;font-size:13px;">array scores[5] = [60, 70, 80, 90, 100]<br>total = 0<br>for i = 0 to 4<br>    total = total + scores[i]<br>next i<br>average = total / 5<br>print("Total: " + str(total))      // 400<br>print("Average: " + str(average))  // 80.0</pre>	Computer Science::Paper 2::Programming Fundamentals
Basic	Write pseudocode to read from and write to a text file.	<pre style="background:#f0f0f0;padding:8px;border-radius:4px;font-family:monospace;font-size:13px;">// WRITING to a file<br>myFile = openWrite("data.txt")<br>myFile.writeLine("Hello World")<br>myFile.writeLine("Second line")<br>myFile.close()<br><br>// READING from a file<br>myFile = openRead("data.txt")<br>while NOT myFile.endOfFile()<br>    line = myFile.readLine()<br>    print(line)<br>endwhile<br>myFile.close()</pre>Always <b>close</b> the file after use to ensure data is saved/released.	Computer Science::Paper 2::Programming Fundamentals
Basic	What are the advantages of using subroutines (functions and procedures)?	<b>1. Code reuse:</b> write once, call many times — avoids duplicating code.<br><b>2. Easier to test:</b> each subroutine can be tested independently (unit testing).<br><b>3. Easier to maintain:</b> fix a bug in one place rather than many.<br><b>4. Shorter code:</b> the main program is cleaner and more readable.<br><b>5. Modular design:</b> large problems broken into manageable, logical units.<br><b>6. Team collaboration:</b> different programmers can write different subroutines simultaneously.	Computer Science::Paper 2::Programming Fundamentals
Basic	Write pseudocode to reverse a string.	<pre style="background:#f0f0f0;padding:8px;border-radius:4px;font-family:monospace;font-size:13px;">function reverseString(s)<br>    reversed = ""<br>    for i = length(s) - 1 to 0 step -1<br>        reversed = reversed + s[i]<br>    next i<br>    return reversed<br>endfunction<br><br>print(reverseString("Hello"))  // "olleH"</pre>	Computer Science::Paper 2::Programming Fundamentals
