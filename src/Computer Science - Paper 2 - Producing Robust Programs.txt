#separator:tab
#html:true
#notetype column:1
#deck column:4
Basic	[Q7a — 4 marks] Give two ways to improve the maintainability of a program. For each, give an example from the circle area program.	Two ways (1 mark name + 1 mark example each):<br><b>1. Comments/annotation</b> — e.g. "line 4 checks the input is valid"<br><b>2. Indentation</b> — e.g. indenting the code within an IF statement<br><b>3. Using constants</b> (also acceptable) — e.g. π (3.142 could be stored as a constant rather than typed each time)	Computer Science::Paper 2::Producing Robust Programs
Cloze	Ways to improve program maintainability: {{c1::Comments/annotation}} — e.g. explaining what line 4 does. {{c2::Indentation}} — e.g. indenting code inside an IF statement. {{c3::Using constants}} — e.g. storing π as a constant (3.142) rather than repeating the literal value.		Computer Science::Paper 2::Producing Robust Programs
Basic	[Q8b(i) — 3 marks] Three parts are missing from this validation code for minutes played (valid range 0 to 300). What are the three missing parts?<br><pre style="background:#1e1e1e;color:#f8f8f2;padding:8px;border-radius:4px;font-family:monospace;font-size:12px;">if mins &lt; 0 ___ mins ___ then<br>    ______("Invalid input")<br>endif</pre>	The three missing blanks are:<br>1. <code>or</code><br>2. <code>&gt; 300</code> (or &gt;= 301)<br>3. <code>print</code><br><b>Python note:</b> <code>or</code> is the same keyword in Python; <code>print("Invalid input")</code> is identical syntax.	Computer Science::Paper 2::Producing Robust Programs
Cloze	Validation for 0–300: <code>if mins &lt; 0 {{c1::or}} mins {{c2::> 300}} then {{c3::print}}("Invalid input") endif</code>. The keyword {{c4::or}} is used because the input is invalid if EITHER condition is true.		Computer Science::Paper 2::Producing Robust Programs
Cloze	[Q8b(ii) — 2 marks] Complete the test plan for minutes validation (valid range 0–300):<br><table border="1" cellpadding="7" style="border-collapse:collapse;font-size:13px;"><tr><th>Type</th><th>Test data</th><th>Expected result</th></tr><tr><td>Invalid/Erroneous</td><td>{{c1::any value &gt; 300 (e.g. 350)}}</td><td>{{c2::Invalid input message displayed}}</td></tr><tr><td>Boundary</td><td>{{c3::300}}</td><td>{{c4::Value accepted}}</td></tr></table>		Computer Science::Paper 2::Producing Robust Programs
Cloze	[Q8f — 4 marks] Complete the test plan for gameName validation (length &gt;0 AND &lt;20):<br><table border="1" cellpadding="7" style="border-collapse:collapse;font-size:13px;"><tr><th>Type</th><th>Test data</th><th>Expected output</th></tr><tr><td>Invalid</td><td>{{c1::string with 0 or 20+ characters}}</td><td>{{c2::"Game name is not valid"}}</td></tr><tr><td>Valid/Normal</td><td>{{c3::string with 1–19 characters}}</td><td>{{c4::"Valid game name"}}</td></tr></table>		Computer Science::Paper 2::Producing Robust Programs
Basic	What are the three types of test data and when is each used?	<b>Normal (valid):</b> data within the acceptable range that the program should accept — e.g. mins = 150 for range 0–300.<br><b>Boundary (extreme):</b> values at the edge of the valid range — e.g. 0 and 300. Also test just outside: -1 and 301.<br><b>Erroneous (invalid):</b> data that should be rejected — out of range or wrong type — e.g. mins = 350 or mins = "hello".	Computer Science::Paper 2::Producing Robust Programs
Basic	What is the difference between a syntax error, a logic error, and a runtime error?	<b>Syntax error:</b> breaks the grammatical rules of the language — the program cannot run at all. Found by the translator.<br><b>Logic error:</b> the program runs but produces wrong results — the algorithm is flawed (e.g. using &lt; instead of &lt;=).<br><b>Runtime error:</b> occurs during execution and causes a crash — e.g. division by zero, index out of bounds.	Computer Science::Paper 2::Producing Robust Programs
Basic	[2024 Q3a — 1 mark] What is a syntax error?	A syntax error is when the code <b>breaks the grammatical rules</b> of the programming language.<br>The program <b>cannot be run</b> — the translator detects it before execution.<br>Examples: missing colon, misspelled keyword, unclosed bracket.	Computer Science::Paper 2::Producing Robust Programs
Basic	[2024 Q3b — 4 marks] Identify two logic errors in this algorithm: Line 01: total=0; Line 02: num1=5; num1=5; Line 03: total=num1+num1; Line 04: if total&gt;=10 AND total&lt;=20 then print("OK"). Write the corrected lines.	<b>Error 1 — Line 02 (or 03):</b> <code>total = num1 + num1</code> should be <code>total = num1 + num2</code> — it adds num1 to itself instead of adding num2.<br><b>Error 2 — Line 04:</b> condition should use correct boundary; acceptable answer: condition uses wrong operator (e.g. &gt;= should be &gt; or &lt;= should be &lt;) depending on specification — any clearly identified condition error accepted.	Computer Science::Paper 2::Producing Robust Programs
Cloze	Logic errors in the total algorithm: Line 03 error — <code>total = num1 + {{c1::num1}}</code> should be <code>total = num1 + {{c2::num2}}</code>. The program {{c3::runs but produces wrong results}} — this is the definition of a {{c4::logic error}}.		Computer Science::Paper 2::Producing Robust Programs
Basic	[2024 Q4b — 2 marks] What is meant by defensive design? Give one example.	Defensive design means writing programs to <b>anticipate and handle potential misuse or errors</b> so the program still works correctly.<br>Examples:<br>• <b>Input validation</b> — checking data is within acceptable range before using it<br>• <b>Authentication</b> — requiring a username/password before allowing access<br>• <b>Sanitisation</b> — removing potentially harmful characters from input	Computer Science::Paper 2::Producing Robust Programs
Cloze	Defensive design means anticipating {{c1::potential misuse or errors}} in a program. Examples include {{c2::input validation}} (checking data is acceptable), {{c3::authentication}} (username/password), and {{c4::sanitisation}} (removing harmful characters from input).		Computer Science::Paper 2::Producing Robust Programs
Cloze	[2024 Q9c(ii) — 3 marks] Complete the test plan for height validation (valid range 40.0–180.0):<br><table border="1" cellpadding="7" style="border-collapse:collapse;font-size:13px;"><tr><th>Type</th><th>Test data</th><th>Expected result</th></tr><tr><td>Normal</td><td>{{c1::e.g. 100}}</td><td>{{c2::"VALID"}}</td></tr><tr><td>Boundary</td><td>{{c3::40.0 or 180.0}}</td><td>{{c4::"VALID"}}</td></tr><tr><td>Erroneous</td><td>{{c5::e.g. 200 or –5}}</td><td>{{c6::"NOT VALID"}}</td></tr></table>		Computer Science::Paper 2::Producing Robust Programs
Basic	[2023 Q5a(i) — 2 marks] Explain why programs should be tested before use.	To <b>identify and correct errors/bugs</b> before users encounter them.<br>To check that the program <b>produces the correct output</b> for all possible inputs.<br>Releasing untested programs risks producing <b>incorrect results</b> or <b>crashing</b>, which could cause harm or data loss.	Computer Science::Paper 2::Producing Robust Programs
Basic	[2023 Q5b — 6 marks] Identify two methods of validation and explain how each can be used on an adding game (two random numbers 1–10, score out of 3).	<b>Method 1 — Range check:</b> Checks the player's answer is within a plausible range — e.g. the answer to adding two numbers 1–10 must be between 2 and 20. Reject values outside this range.<br><b>Method 2 — Type check:</b> Checks the player has entered a number (integer) rather than a string/letter. Display an error message if non-numeric input is entered.	Computer Science::Paper 2::Producing Robust Programs
Cloze	Two validation methods for the adding game: {{c1::Range check}} — verifies the answer is between {{c2::2 and 20}} (sum of two numbers 1–10). {{c3::Type check}} — verifies the input is a {{c4::number/integer}} not a string.		Computer Science::Paper 2::Producing Robust Programs
Basic	[2022 Q4a — 2 marks] Give two ways to improve the maintainability of Jack's program that inputs 5 numbers.	Acceptable answers (any two):<br>• Use <b>meaningful/descriptive variable names</b> — e.g. rename a, b, c, d, e to number1, number2 etc.<br>• Add <b>comments</b> to explain what the code does<br>• Use <b>indentation</b> to make the structure clear<br>• Use a <b>loop</b> instead of repetitive code (makes it easier to change the quantity)	Computer Science::Paper 2::Producing Robust Programs
Cloze	Maintainability improvements: use {{c1::meaningful/descriptive variable names}} (e.g. number1 instead of a); add {{c2::comments}} to explain the code; use {{c3::indentation}}; use a {{c4::loop}} instead of repetitive input lines.		Computer Science::Paper 2::Producing Robust Programs
Cloze	[2022 Q5b(ii) — 3 marks] Complete the test plan for hotel nights validation (valid range 1–5 inclusive):<br><table border="1" cellpadding="7" style="border-collapse:collapse;font-size:13px;"><tr><th>Type</th><th>Test data</th><th>Expected result</th></tr><tr><td>Normal</td><td>{{c1::2}}</td><td>{{c2::ALLOWED}}</td></tr><tr><td>Boundary</td><td>{{c3::1 or 5}}</td><td>{{c4::ALLOWED}}</td></tr><tr><td>Erroneous</td><td>{{c5::0 or 6}}</td><td>{{c6::NOT ALLOWED}}</td></tr></table>		Computer Science::Paper 2::Producing Robust Programs
